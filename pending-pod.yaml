apiVersion: v1
kind: Pod
metadata:
  name: pending-pod
spec:
  containers:
  - name: nginx
    image: nginx:latest
    resources:
      requests:
        memory: "100Gi"
        cpu: "50"
      limits:
        memory: "100Gi"
        cpu: "50"
